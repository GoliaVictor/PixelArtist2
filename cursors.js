let BRUSH_CURSOR = new Icon(BRUSH_ICON).fillColour(Color.inverse);
let ERASER_CURSOR = new Icon(ERASER_ICON).fillColour(Color.inverse);
let DROPPER_CURSOR = new Icon(DROPPER_ICON).fillColour(Color.inverse);
let BUCKET_CURSOR = new Icon(BUCKET_ICON).fillColour(Color.inverse).flipHorizontally();
let ZOOM_CURSOR = new Icon(ZOOM_ICON).fillColour(Color.inverse);